# SkillPod Configuration
# ----------------------
# Variables prefixed with SKILLPOD_ are SkillPod-specific.
# API keys use standard names for compatibility with other tools.

# Path Settings
# SKILLPOD_SKILLS_DIR=./.agent/skills
# Explicit DB override (optional). By default derived from ~/.skillpod/indexes/
# SKILLPOD_DB_PATH=~/.skillpod/indexes/default/skills.lancedb

# Embedding Settings
SKILLPOD_EMBEDDING_PROVIDER=none  # none, openai, gemini

# API Keys & Models (standard names, compatible with other tools)
# OPENAI_API_KEY=sk-...
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# GEMINI_API_KEY=...
# GOOGLE_API_KEY=...  # Alternative to GEMINI_API_KEY
# GEMINI_EMBEDDING_MODEL=gemini-embedding-001

# Search Settings
# SKILLPOD_SEARCH_LIMIT=10
# SKILLPOD_SEARCH_THRESHOLD=0.2

# Skill Filters (comma-separated)
# SKILLPOD_ENABLED_SKILLS=skill-a,skill-b
# SKILLPOD_ENABLED_CATEGORIES=development,documentation
# SKILLPOD_ENABLED_NAMESPACES=team-tools

# Execution Settings
# SKILLPOD_ALLOWED_COMMANDS=python,python3,uv,node,cat,ls,grep

# GitHub (for private repositories)
# GITHUB_TOKEN=ghp_...
